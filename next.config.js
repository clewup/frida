/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
        'isku.fi',
        'i.ibb.co',
        'images.unsplash.com'
    ]
  },
}

module.exports = nextConfig
